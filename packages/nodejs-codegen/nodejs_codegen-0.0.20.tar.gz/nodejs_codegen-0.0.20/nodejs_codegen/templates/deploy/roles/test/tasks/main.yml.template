---
- name: query by project
  uri:
    url: "{{'{{'}}{{name}}_test_url{{'}}'}}/act"
    method: POST
    body_format: json
    return_content: yes
    body: {
      "action": "search_by_project",
      "param": {
         "project": "alancao"
        }
    }
    register: result

- debug: var=result.content

...