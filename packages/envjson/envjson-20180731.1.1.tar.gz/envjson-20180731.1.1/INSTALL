envjson installation
====================


Python ``pip`` package is required to install ``envjson``.


From sources
------------

Use the following command line to install ``envjson`` from sources directory (containing setup.py):

    pip install .

or

    python setup.py install


From PyPI
---------

Alternatively you can install ``envjson`` from PyPI:

    pip install envjson


Use `-U` flag for upgrade:

    pip install -U envjson

