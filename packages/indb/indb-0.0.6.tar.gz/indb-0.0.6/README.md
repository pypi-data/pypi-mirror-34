# Infinity-DB

`Managing objects should be as easy as managing bits.`


This is a test project, to define the data types in `metadb` language.

It enables reading app data sources, that provide `SCANME.md` file.

```pip install indb```

## Loading data
`indb scan <source>`
